{
  "name": "emergency-exit",
  "private": true,
  "description": "Emergency Exit — digital legacy vault",
  "scripts": {
    "test:frontend": "npx playwright test --reporter=list",
    "test:frontend:ui": "npx playwright test --ui",
    "test:frontend:headed": "npx playwright test --headed",
    "serve": "npx serve . -l 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.0"
  }
}
